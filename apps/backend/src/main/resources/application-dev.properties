# Local convenience only. The test and production profiles do not import a
# developer workstation's root .env unless a test explicitly also activates dev.
spring.config.import=optional:file:../../.env[.properties]
