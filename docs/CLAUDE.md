# Koupreng Invitation Project — Instructions for Claude

## User Identity & Communication
- Name: Nha (IT Year 4, BBU Battambang)
- Understand Romanized Khmer (e.g. `nh`=ខ្ញុំ, `jg`=ចង់, `mx`=មិច, `ot`=អត់, `tver`=ធ្វើ, `brer`=ប្រើ, `oy`=ឱ្យ, `dg`=ដឹង, `hxx`=ហើយ, `dak`=ដាក់, `mer`=មើល, `ban`=បាន, `te`=ទេ, `skol`=ស្គាល់, `doch`=ដូច, `tx`=តិច, `pg`=ផង).
- Reply in Khmer + English technical terms.
- Short, direct, practical answers. No unnecessary long theory.

## Project Overview
- Monorepo: `apps/backend` (Spring Boot / Java), `apps/frontend-user` (React + Vite), `apps/frontend-admin` (React + Vite).
- Ports:
  - Backend API: `http://localhost:8080`
  - Frontend User: `http://localhost:5173`
  - Frontend Admin: `http://localhost:5174`
